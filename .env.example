# Vite exposes only variables prefixed with VITE_
# https://vite.dev/guide/env-and-mode.html

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Chats sync to Postgres after sign-up. In Supabase: Authentication → Providers → enable Email.
# Run SQL migrations (companion_profiles + chat_threads / chat_messages + RLS).
